/**
 * Site-wide constants for the Deal Signals app.
 */

export const GUIDE_COUNT = 0;

export const GUIDE_CATEGORY_COUNTS: { id: string; label: string; count: number }[] = [];

// ─── TERMINOLOGY ──────────────────────────────────────────────────
export const TERMS = {
  guides: "Guides",
  articles: "Articles",
} as const;
